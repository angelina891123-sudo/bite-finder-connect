# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Secrets — .env holds SUPABASE_SERVICE_ROLE_KEY, which must never be committed.
# See .env.example for the expected keys.
.env
.env.*
!.env.example

# Leftovers from the old dev-only local photo storage; campaign photos now live
# in the Supabase "campaign-photos" bucket. Safe to delete these folders.
/local-data/
/public/uploads/

# Supabase CLI local state (caches the linked project ref and pooler URL)
supabase/.temp/

# Local tooling config
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
